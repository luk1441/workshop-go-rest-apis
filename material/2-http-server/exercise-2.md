# Exercise 2 – Create a New HTTP Handler

**Goal:**  
Extend the Go HTTP server by creating a new handler function (`exampleHandler`) that responds to requests on `/example`.  

The handler should print (`Hello from the example handler!`)
